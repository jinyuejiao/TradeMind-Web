/**
 * 开发规范补充：跨端（Capacitor）
 *
 * 1. 业务只改 TradeMind-Web；App 仅 npm run sync
 * 2. 原生能力只通过 window.TM_Native
 * 3. App API 地址用 TM_APP_API_BASE 或正式域名（env-config.js）
 * 4. 新增页面保持 modules/ + assets/js/ 约定与响应式断点 768px
 */
